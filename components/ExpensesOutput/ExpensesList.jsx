import { StyleSheet, View } from "react-native"

const ExpensesList = ({ expenses, periodName }) => {
    return (
        <View></View>
    )
}

export default ExpensesList

const styles = StyleSheet.create({

})